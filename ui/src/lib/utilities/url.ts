export const apiUrl = 'http://127.0.0.1:7654';

export const APIRoutes = {
  initiate: `${apiUrl}/initiate`,
  fromAccount: `${apiUrl}/from-account`,
  toAccount: `${apiUrl}/to-account`,
  amount: `${apiUrl}/amount`,
  schedule: `${apiUrl}/schedule`,
}